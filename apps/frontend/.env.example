# ── Frontend (próximas variables públicas) ─────────────────────────────────
# URL base de la API del backend. En producción apunta al dominio de Render.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# reCAPTCHA de Google (debe coincidir con el RECAPTCHA_SECRET_KEY del backend)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=